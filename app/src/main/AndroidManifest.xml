<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- android:name sets the name that appears on the device app icon -->
    <application
        android:name="playground.main.MyApp"
        android:allowBackup="true"
        android:supportsRtl="true">

    <!-- adjustResize ensures that the main window resizes to make room for the soft keyboard-->
    <!-- android:name must match package name of activity beginning from namespace defined in build.gradle file -->
    <activity
        android:name=".ui.MainActivity"
        android:exported="true"
        android:windowSoftInputMode="adjustResize">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>
</manifest>
